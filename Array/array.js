let JsArray = [1, 24.24, 3, 4, 5, 6, 7, 8, 2.25];
//console.log(JsArray);
//console.log(JsArray[1] + JsArray[3] + JsArray[5]);
JsArray.push(2.1);

let totaal = JsArray.pop(); + JsArray.pop();
let waarde1 = JsArray.pop();
let waarde2 = JsArray.pop();

console.log(totaal);

JsArray = 'banaan'

for (let i = 0; i < JsArray.length; i++) {
    //console.log(JsArray[i]);
}

